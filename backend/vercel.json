{
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.ts" }
  ],
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "crons": [
    {
      "path": "/email/late/send",
      "schedule": "*/2 * * * *"
    }
  ]
}
